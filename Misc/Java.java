// BigInteger
gcd, modPow, modInverse, not, and, or, xor, andNot, shiftLeft(int), shiftRight(int), clearBit(int), setBit(n), flipBit(n), testBit(n), bitBount, bitLength, getLowestSetBit, equals, signum, doubleValue, intValue, longValue, toString(base)
// Regex
import java.util.regex.*
Pattern pattern = Pattern.compile(regex);
Matcher matcher = pattern.matcher(string);
Matcher functions:
matches(), find(), find(start), group() // returns input subsequence matched by previous match (find?), replaceAll(), replaceFirst()
// DecimalFormat
import java.text.*
DecimalFormat df = new DecimalFormat("0.00###E0"); // #=skip if possible
